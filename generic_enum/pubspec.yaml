name: generic_enum

description: Dart enumerations with extension-methods offering
  json-serialization and a mapping of each enum instance to a const value with
  arbitrary data-type.

version: 0.4.1

homepage: https://github.com/simphotonics/generic_enum/tree/main/generic_enum

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies: null

dev_dependencies:
  exception_templates: ^0.2.1
  pedantic: ^1.11.0
  test: ^1.16.8
